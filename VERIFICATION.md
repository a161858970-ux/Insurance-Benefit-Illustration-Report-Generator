# VERIFICATION —— 验证报告（方向二）

> **状态：已完结，总控终验通过（2026-09-25）。** 作业要求覆盖 6 项：做了什么 / LLM 用在哪几步 / 提示词改了哪几版、关键改动是什么 / 校验结果 / 修了什么问题 / 还有什么没解决。逐节对应第 1–6 章；合规清单逐条核对见第 7 章；复验命令见第 8 章；**总控终验结论见第 9 章**。

## 1. 做了什么

**目标（作业方向二）**：做一个利益演示报告生成器，两个能力——通用渲染（任意一款产品自动出完整展示、不为每款单独写代码）+ 个性化报告（同一份数据换客户画像出不同版本），且数字不经过 LLM。

**最终产物**：
| 产物 | 数量 | 位置 |
|---|---|---|
| 单场景报告（对拍场景） | 2 份 | `output/m1`、`output/m1_30y10` |
| 7 产品通用渲染报告 | 7 份 | `output/m2/*` |
| 3 画像个性化对比组 | 3 份 | `output/m3/cmp_p_*` |
| H5 交互页 | 1 套 | `output/h5/index.html` + `data.json` |
| 合成第 8 字段组合自测产物 | 1 套 | `validation/synthetic/*` |
| 提示词档案 | 9 个 | `prompts/`（narrative v1–v6、reorder v1、narrative_modular v1–v2） |
| 校验代码 | 7 个脚本 | `validation/*.py` |

**架构（两层严格分离，SPEC §6 数字纪律）**：
- 渲染层（纯标准库 Python，不经 LLM）：解析 JSON → 五类边界年扫描 → 勾稽校验 A/B/C → 摘要与三元组登记 → SVG 图 → 模块块组装 → 页脚/免责拼接 → 六道输出层断言 + 数字回引
- 表达层（MiMo LLM）：只做四件事——从摘要找亮点写叙述、按画像输出模块顺序（reorder）、每模块一句画像化解读（narrative_modular）、违规时合规改写

**12 份报告统一口径**：单段 2 份（m1 对拍场景）+ 7 产品 + 3 画像，全部走同一条 `m1_run`/`modular_run` 管道，全部含免责声明与数据来源/获取时点页脚。

## 2. LLM 用在哪几步

LLM 只在表达层出现，共 4 个调用点（全部输入是代码生成的摘要，输出全部过回引+断言）：

| 步骤 | 提示词文件 | 输出 | 校验 |
|---|---|---|---|
| ① 单段叙述（M1/M2 管道） | `prompts/narrative_v6.md` | 一段面向客户的说明 | 数字回引（三元组）+ 六道断言 + 禁用词扫描，违规改写 ≤2 轮 |
| ② 模块顺序（M3/M4/M5） | `prompts/reorder_v1.md` | JSON 数组（8 个模块 ID 的排列） | 白名单排列校验，非法回退默认序，`m_disclaimer` 强制末位 |
| ③ 模块句（M3/M4/M5） | `prompts/narrative_modular_v2.md` | JSON：lead + 每模块一句 | 逐句回引 + 六道断言，surgical 改写 ≤3 轮（只改被指出的句子） |
| ④ 合规改写 | 复用 ①/③ 的 prompt +【合规重写】追加块 | 修正后的文本/JSON | 改写后重扫全部断言 |

M5 起在 ②③④ 之前新增**句查轮**（D058）：lead + 全部模块句（含从不渲染的 m_disclaimer 句）先过句子级三查（禁词/档位窗口/区间/源泄漏/回引，剔除对片段必然误报的文档级 coverage 与保障责任），违规则改写循环复检 ≤3 轮，句查不过则该份判不通过。

**LLM 绝对不碰的**：任何数字的产生/计算/格式化/换算/取舍、边界年、勾稽、图表、模块数据块、免责与页脚拼接、档位标注（由 `tier_of()` 机器生成）。IRR/单利/收益倍数只引用不重算。

**模型与调用参数**（`config.yaml`，D043/D050）：`mimo-v2.6-flash`；`enable_thinking=false` 全局关思考（长约束 JSON 任务 reasoning 会爆），改写轮 per-call `thinking=True` 质量优先。key 只在 `config.yaml`（不入库、不走环境变量）。

## 3. 提示词改了哪几版、关键改动是什么

全部留档在 `prompts/`，v1→v6 逐版可 diff：

| 版本 | 关键改动 | 为什么改（决策号） |
|---|---|---|
| narrative v1 | 初版：任务+硬性约束（不重算、两档分开、演示标不保证、禁用词、附免责） | 起点 |
| v2 | 重试轮同样完整生效的硬性约束块 + IRR 固定写法 + 时点数字豁免 + 禁止自行断言领取截止 | v1 实测重试轮引入禁词"收益率"、年龄数字被回引误伤（D014/D015） |
| v3 | 免责改由系统附加、合计必须带区间标签引用、档位照抄摘要标注 | 免责重复、"到105岁"区间错误、档位手写污染（D018–D022） |
| v4 | 交费口径规则（年交只能引保费行）、纯文本禁 Markdown、输出层档位显式约束 | "0 岁宝宝"配 30 岁场景、累计 100,000 被说成年交 10 万（D024–D028） |
| v5 | 锚改 assert 化（揪出 v2 静默未命中事故）+ 禁数据外功能断言 + 合计同句带区间 | 提示词版本替换静默失效，M1 后期实际一直用 v2（D034） |
| v6 | 禁"保证利益演示"措辞 | 该词组让档位窗口规则失效，3 轮改写不收敛（D040） |
| reorder v1 | 结构化 JSON 数组 + 白名单排列 + 非法回退 | 顺序由 LLM 定、数字块不经 LLM（D041） |
| narrative_modular v1→v2 | v2 外科手术式改写协议（只改被指出的句子，lead 与其余逐字保留） | 整稿改写会误伤已通过的句子（D041、M3 commit 23df8e8） |

M5 本轮**没有新增提示词版本**——页脚/过滤/通用性都是代码侧改动（D053–D057），LLM 的四步骤输入输出未变，提示词仍是上表终版。

## 4. 校验结果

### 4.1 七个验证脚本（fresh 子进程逐个跑，M5 收口复跑、总控终验再复跑）

| 脚本 | 检查内容 | 结果 |
|---|---|---|
| `tier_assert_intercept.py` | 档位断言坏样本拦截 | **exit=0**，坏样本 2/2 拦截、好样本放行 |
| `adversarial_trace.py` | 同值多字段对抗回引 | **exit=0**，8/8（含"无语境同值必须 AMBIG"） |
| `output_tier_lint_test.py` | 档位窗口/区间绑定/标量归属 | **exit=0**，9/9 |
| `synthetic_combination_test.py` | 合成第 8 字段组合零改码 | **exit=0**，全管线通过（勾稽 A105/B105/C208、摘要自回溯 101/0） |
| `coverage_source_test.py` | 给付流覆盖/源标注/**页脚坏样本 5 例（M5）**/**句子级三查坏样本 3 例（M5）** | **exit=0**，14/14（6 旧 + 5 页脚 + 3 句查） |
| `h5_lint_test.py` | H5 回溯/硬编码/页脚/交互机制 | **exit=0**，6/6（含页脚断言、产品场景自读） |
| `final_verify.py` | 12 份报告全量终验 | **exit=0**，12/12 全部通过 |

### 4.2 12 份报告终验明细（`final_verify.py` 实跑输出）

```
12 份逐份：回引 unmatched=0 | 禁词=0 | 档位=0 区间=0 覆盖=0 源标注=0 | 免责 块=1/残留=0 | 页脚=√ | md=False -> 通过
共 12 份报告；总判定: 全部通过（FINAL_VERIFY_EXIT=0）
```

### 4.3 勾稽基线（84 文件全量，独立重算）

```
文件数=84
A=6720/6720  B=5760/5760  C=12600/12600  失败=0
基线 A=6720/6720 B=5760/5760 C=12600/12600 失败=0: 一致 √
```

### 4.4 数据来源与获取时点（作业合规清单第 2 条，M5 主修项）

- 修复前基线：12/12 终稿**均无**数据来源句（实测确认总控判断）
- 修复后：页脚两行由代码拼接（不走 LLM），**12/12 全含**；`source_lint(require_footer=True)` 断言 + 5 个坏样本（缺页脚拦 / 半截页脚拦 / 齐全放行且"数据来源："子串不误杀 / 正文泄漏仍拦 / 片段级不强求）
- 页脚豁免出回引与档位窗口：实测发现三个坑（`源：` 子串误杀、文件名"30，10"并成无出处 token、"利益演示数据"含"演示"误触档位窗口且 LLM 改写修不掉），由 `strip_footer` 统一解决
- H5 同步：`data.json.footer` 两行 + 页面渲染，`h5_lint_test` 增页脚断言

**连带挖出的 lint 死角（D058）**：h5_build 首次带页脚重建时拦到 p_analyst 的 `m_disclaimer` 模块句 tier 违规——该模块 `module_block` 恒返 None、句子从不渲染，m3 终验只查渲染正文，违规句因此入库。修法：`probe_violations` 句子级三查（剔除文档级 coverage/responsibility，二者对片段必然误报）+ m3 probe 循环复检 + modular_run 补句查轮 + 终态挂 `probe_ok` + h5 只入会渲染句子；坏样本 3 例含实抓句。12 份 manifest 句级重扫：1 脏（p_analyst）→ 重跑归净。

### 4.5 m3 三份 flash 重跑（总控拍板项）

- 重跑前：3 份为 mimo-v2.5-pro 产物（README 已知缺陷第 2 条）
- 重跑后：`model=mimo-v2.6-flash`，3/3 终验通过，order/lead/模块句全部为 flash 新产物；两处改写轮补 `thinking=True`（对齐 D050）
- **重跑中发现并修复**：`profiles.jsonl` 在 M4 加了 `p_default`，m3_compare 未过滤 → 多产出第 4 份 `cmp_p_default`、且"3/3 通过"是硬编码字样会说谎。修复：`p_default` 标 `compare:false`、m3 过滤、`len(results)==3` 入判定（D057）；多余产物已清理，12 份口径恢复
- 现状：12 份产物模型全为 flash，无混代

### 4.6 H5 通用性（换产品零改码，M5 任务 2）

同一 `index.html`（sha256 前后完全一致 `6bfc0f3a1db91b4d4e2110fd5c7c4a5449c173c02f2dcc5e61b46994d9bab628`），只换 `data.json`：

| | 页面标题 | 表格首行 `[年龄, 累交保费, 现金价值, 生存总利益保证, 生存总利益演示, 身故保险金]` |
|---|---|---|
| 换之前 | 国寿鑫益延年养老年金保险（分红型） | `[31, 10,000, 2,790, 2,790, 2865.8, 10,000]` |
| 换之后 | 中意永续我爱（睿享版）终身寿险（分红型） | `[1, 10,000, 8,932, 8,932, 9040.43, 10,000]` |

- 换产品命令：`python src/h5_build.py "<产品名>" "<场景文件名>"`（中意为增额终身寿、无领取流，与鑫益延年形态完全不同）
- 换后 `h5_lint_test` 6/6 全过（回溯 525/525 + 23/23、页脚√、0 硬编码、交互机制√）
- 页脚随产品自动切换（浏览器实读到中意的产品名+场景文件名）
- 换产品时 m3 manifest 不匹配 → 画像自动回退默认序+空句（D056），不会张冠李戴
- 测后已恢复默认 data.json（标题回到鑫益延年）

### 4.7 真人浏览器冒烟（总控补做，两轮均记录在案）

**第一轮（M4 验收，v2.5-pro 产物）**：本地服务起页真人点过——75 行表格 / 444 个 data-source 节点 / 5 条序列线；切画像三份两两不同且 lead 与模块句同步变；切年度高亮精确跟随（60→`['60']`、105→`['105']`，关键值卡 6/34→14/34）；60 岁行 5 个数字与 `data.json` 真值完全一致（100,000 / 144,450 / 147,640 / 192893.91 / 144,450）。

**第二轮（M5 终验，flash 产物）**：m3 三份 flash 重跑后 order/lead/句全变，总控重新起服务点过——切画像三份模块序两两不同（`basics→guarantee→cashflow…` / `irr→guarantee→dividend…` / `basics→cashflow→claims…`）、lead 三不同；切年度高亮行 60→`['60']`、105→`['105']`、40→`['40']` 精确跟随；关键值卡 60岁=6、105岁=14、40岁=5，**与总控独立从 `data.json` 算出的应高亮数逐个相等**；页脚三行（免责/数据来源/获取时点）齐。本项目侧验证为静态+机制级（`h5_lint_test`），两轮真人冒烟均由总控完成。

### 4.8 全残分支回源核对（总控 M4 验收）

三份全残产物逐字回源一致：中意@`1=10000`@`105=58649`、鑫颐金生@`105=544050`、福临门@`31=10000`，终稿均含"全残"。M5 重跑后 M4-2 检查复跑仍 3/3 √。

## 5. 修了什么问题

按发现顺序（每条都有坏样本或实测证据）：

1. **重试轮引入禁词**（v1→v2，D014）：约束块改为每轮完整生效
2. **年龄数字回引误伤**（D015）：去逗号 core 索引 + 整数豁免
3. **扫描只报告不修复**（D016）：加合规改写闭环
4. **HTML 对拍 2 处舍入**（D017）：字面命中/数值关系分列如实标注
5. **[blocker] 摘要层档位污染**（D018）：档位归属表进 SPEC，`tier_of()` 机器生成，坏样本 2/2 拦截
6. **"到105岁"区间错误**（D019）：代码产出区间标签，LLM 只准引用
7. **值对出处张冠李戴**（2.65/4.22 系统性，D020）：回引升级三元组 + 万元换算绑定字段 + AMBIG 判失败，对抗 8/8
8. **免责正文+免责块重复**（D021/D027）：代码剥句后统一附加，断言残留=0 免责块=1
9. **倍数单位拼成"元"**（D023）：add 支持 unit
10. **画像硬编码配错场景**（D024）：画像按 meta 动态生成
11. **累计保费说成年交**（D025）：摘要加保费行 + 交费口径规则
12. **输出层档位混排**（D026）：窗口规则，"根据演示…（保证）"被拦
13. **`60-104` 连字符被切**（D029）：trace 前 + sanitize 双处归一
14. **标量归属巧合匹配**（D031）：标量入池 + 紧邻岁/年消歧
15. **合计脱离区间标签**（D032）：range_binding_lint 同句断言
16. **消歧平局/跨行污染**（D033/D049）：五级规则定稿、全部限定 token 行内，对抗 8/8
17. **提示词版本静默未命中**（D034）：锚改 assert 化（M1 后期实际一直用 v2 的隐形事故）
18. **场景优先级失效 + 全角逗号 IndexError**（D035）：tail 外层循环、re.split 兼容半/全角
19. **给付流漏呈现**（长城漏年金流、福临门漏特别生存金，D037）：全枚举进摘要 + coverage_lint，坏样本 6/6
20. **内部源标注泄漏**（福临门 9 处，D038）：sanitize 统一剥 + source_lint
21. **单点流"1"系统性误伤**（D039/D048）：单点流跳过区间绑定，走 coverage
22. **"保证利益演示"让窗口规则失效**（D040）：prompt v6 禁该措辞
23. **思考模型截断致空回复**（D041）：max_tokens 给 reasoning 预算
24. **趸交 hints 平局 3 轮不收敛**（D047）：趸交特征词入 hints
25. **`bool('false')=True`**（D050）：config 真布尔解析
26. **[M5] 数据来源/获取时点 12/12 全缺**（总控核出，D053）：页脚代码拼接 + source_lint 双向断言 + 5 坏样本；连带修三坑（`源：` 子串误杀、文件名数字无出处、档位窗口误触）
27. **[M5] m3 三份混 v2.5-pro**（D054）：flash 全量重跑，12 份模型统一
28. **[M5] m3 多产第 4 份 + "3/3"硬编码说谎**（重跑时发现，D057）：`p_default` 标 `compare:false`、数量入判定、多余产物清理
29. **[M5] H5 三处硬编码产品/场景**（D055/D056）：命令行参数 + 画像不匹配回退 + lint 自读 data.json
30. **[M5] lint 死角：从不渲染的句子绕过终验**（h5_build 暴雷，D058）：`module_block` 对 `m_disclaimer` 恒返 None → 该模块句从不进渲染正文 → m3 终验只查渲染正文 → p_analyst 的 m_disclaimer 句带 tier 违规（"保证与演示差额由IRR复利1.65%与2.51%量化"，1.65 为保证档）入库，直到 h5 汇总可见文本才被拦。修：新增句子级三查 `probe_violations`（剔除对片段必然误报的文档级 coverage/responsibility），m3 probe 改循环复检、modular_run 补句查轮、终态 ok 挂 probe_ok、h5 只入会渲染句子；坏样本 3 例（含实抓死角句）。12 份 manifest 句级重扫 1 脏 → 重跑归净

## 6. 还有什么没解决

如实列出，不藏：

**总控指定必录 4 条：**
1. **AMBIG 消歧是启发式**：同值 + 同语境 + 无源声明的极端组合仍会判失败（AMBIG→重试→改写，3 轮不收敛则该份不通过）。这是刻意选择——宁可失败也不乱挑出处；语义级消歧未做。
2. **关思考的 flash 首版偶发不带字段名的句子**：靠改写轮兜底（改写轮开思考），首版偶尔要多跑 1 轮。
3. **轻任务误配 thinking 会把秒级调用拖成分钟级**（M5 实测并已修，非遗留）：D058 句查轮最初沿用 D050 的"改写开思考"，实测同一调用 `thinking=True` **495.2s** vs `thinking=False` **6.0s**（输出质量相当）——因为句查是单句 surgical 修改、不是整稿长 JSON 重写。曾因此两次误判"挂死"并杀进程。现句查轮固定关思考，整稿终验改写仍开思考（D050 结论对该场景仍成立）。**仍属环境性的一点**：API 偶发空回复/慢滴答会拖时间，client 的 90s 是 socket 级超时、缺"总时长硬上限"，慢滴答响应可使其失效。
4. **H5 是静态+机制验证**：h5_lint_test 断言回溯/硬编码/交互机制；真人冒烟已由总控补做（4.7 记录），项目侧未做跨浏览器/移动端适配测试。

**项目侧自查另列：**
5. **D030 同值跨量纲归属**：如"10 年"vs"10 万元换算"，值本身可溯源无假，但归属标注语义可能偏（如把交费期间标成换算）。M2 后已用标量优先规则缓解，未做语义级消歧。
6. **特别生存金勾稽未建模**：A/B/C 三条公式覆盖生存总利益/累交/红利，特别生存金只进 coverage 呈现断言、没有独立勾稽恒等式（源数据未给可校验关系）。
7. ~~**m3 对比组 flash 重跑后未再做真人切画像复验**~~ —— **已关闭（总控 M5 终验）**：flash 版 H5 已由总控起本地服务开浏览器真人复验，切画像三份模块序两两不同、lead 三不同；切年度高亮行 60/105/40 精确跟随，关键值卡 6/14/5 与独立从 `data.json` 算出的应高亮数逐个相等；60 岁行 5 个数字与真值一致。
8. **通用性证明覆盖 7 产品 txt + 2 产品 H5**：H5 只实测了鑫益延年（默认）与中意（换品），其余 5 产品未逐一换入 H5（渲染层同源，风险低但未穷举）。
9. **单利/收益倍数口径非年化**：已在行文强制标注"非年化非利率"，但读者仍可能误读为年化收益；无代码层可断言的改进空间，属表达局限。
10. **H5 无失败态**：`fetch('data.json')` 失败时页面静默空白，无用户可读的错误提示（静态页，未加）。

## 7. 作业合规清单逐条核对（作业说明 PDF 尾部）

| # | 条目 | 状态 | 证据 |
|---|---|---|---|
| 1 | 输出物都有免责声明 | √ 12/12 | `final_verify` 每份断言"免责块=1、正文残留=0" |
| 2 | 标注了数据来源和获取时点 | √ 12/12 + H5 | M5 页脚，`source_lint(require_footer=True)` + 5 坏样本（4.4） |
| 3 | 没出现「推荐」「建议购买」「适合您」 | √ 0 命中 | `banned_scan` 词表含这 3 词，12 份终稿实扫 0 |
| 4 | 没有对具体公司或产品作优劣结论 | √ 0 命中 | 12 份终稿扫优劣词（最优/优于/胜过/最划算等）0 处；prompt 约束"不评价产品好坏" |
| 5 | 成果不对外传播、不用于实际销售 | √（判断依据见下） | **数据源未传播**：课程所给 `利益演示数据/` 原始 JSON **入库 0 个**（135 个入库文件中无任何材料目录文件），仓库不含数据源本身。**公开的是**：代码、文档、提示词档案与由代码生成的报告文本（`output/` 24 个文件，含 H5 `data.json`，其中含派生数字）。**判断依据**：仓库设为 GitHub 公开是提交方式（老师以仓库链接收作业，免打包 zip 手工提交），非成果传播；不作宣传、无对外发布渠道；免责声明在 12 份报告与 H5 页脚均明示"仅供教学研究使用"；产物不用于任何销售场景。完整决策记录见 D060 |
| 6 | 保证与浮动分开呈现，两档同屏 | √ | SPEC §2.5 档位表 + `tier_lint` blocker + 模块"三、保证利益 / 四、演示利益"分块 |
| 7 | 演示部分标注了「演示、不保证」 | √ | 档位词由 `tier_of()` 机器生成（保证档/演示、不保证），禁手写；输出层窗口断言兜底 |
| 8 | 禁用词表用代码扫过 | √ 0 命中 | `banned_scan` 11 词表，`final_verify` 每份必扫，7 脚本全绿 |
| 9 | 没把单利、收益倍数当收益率用 | √ | 禁用词含"收益率"（0 命中）；单利/倍数行强制标"非年化非利率"；IRR 唯一收益口径 |
| 10 | 没有修改、重算、推算任何给定数字 | √ | 回引 unmatched=0（12 份）；LLM 输入只有摘要、输出逐数回溯源三元组；勾稽失败=0 证明未改数 |

（清单共 13 行、方向二适用上表 10 行；方向一专属的"对比表只陈述条款/结论追条款条号/未载明字段为 null"3 行不适用方向二。）

## 8. 怎么复验（全部命令 fresh 子进程）

```bash
# 7 个验证脚本（全 0 才算过）
python validation/tier_assert_intercept.py      # 2/2 拦截
python validation/adversarial_trace.py          # 8/8
python validation/output_tier_lint_test.py      # 9/9
python validation/synthetic_combination_test.py # 第8组合零改码
python validation/coverage_source_test.py       # 14/14（6 覆盖/源 + 5 页脚 + 3 句查）
python validation/h5_lint_test.py               # 6/6（回溯/硬编码/页脚/交互）
python validation/final_verify.py               # 12/12 终验

# 产物重生成（换模型/改代码后必须重跑，不许沿用旧结论）
python src/m3_compare.py    # 3 画像对比组（flash）
python src/m4_run.py        # 9 份模块化（flash）
python src/h5_build.py      # H5 data.json（含六道 lint）
```

勾稽基线（A=6720/6720、B=5760/5760、C=12600/12600、失败=0）由 84 文件全量独立重算，见 4.3。

## 9. 总控终验结论（2026-09-25 · 项目完结）

**结论：M1–M5 全部通过，交付物齐备，达到作业"优秀"档要求，可提交。**

终验由总控独立执行，不采信执行侧自述，逐项复核结果：

| 复核项 | 方法 | 结果 |
|---|---|---|
| 7 个验证脚本 | fresh 子进程逐个实跑 | exit 全 0（`coverage_source_test` 14/14） |
| 12 份报告终验 | `final_verify.py` 实跑 | 12/12 全过（回引 0 / 禁词 0 / 档位·区间·覆盖·源标注 0 / 免责 1·0 / 页脚 √） |
| 勾稽基线 | 总控独立重算 84 文件 | A=6720/6720、B=5760/5760、C 保证 6300+演示 6300=12600/12600，失败 0，与声明一致 |
| 数据来源页脚（第 2 条） | 12 份逐份查 + 文件名与 manifest 比对 | 12/12 两行齐、文件名逐份一致、源文件在磁盘真实存在 |
| 合规清单方向二 10 条 | 总控独立扫描 12 份终稿 + H5 可见文本 | 逐条 √（第 5 条判断依据见第 7 章） |
| H5 真人冒烟 | 起本地服务开浏览器实测（两轮） | 见 4.7；flash 版高亮卡数与独立计算值逐个相等 |
| 模型一致性 | config + 产物核查 | 12 份统一 `mimo-v2.6-flash`，无混代 |
| git 三项 | `rev-parse` / `ls-remote` / `ls-files` | 本地 HEAD = 远端 HEAD = `a342f30`；工作区干净；`config.yaml` 未入库、已跟踪文件无 `sk-` 密钥 |
| 交付五件套 | 齐套性核查 | README / SPEC / DECISIONS(D001–D060) / CHANGE_LOG / VERIFICATION + prompts 9 个 + 校验代码 7 个 |

**终验中总控更正的自身误判（留档）**：M1 报"残留到105岁"（字符串匹配过粗，执行侧②是对的）、M2 报"77,140.8 不可回引"（值池漏了代码派生合计值）、M4 判定脚本报"三画像 order 不同=False"（浏览器 tab 状态残留）。三次均为总控侧口径/状态问题，非项目缺陷，已在当轮更正。

**遗留问题**：见第 6 章，10 条中第 3、7 条已关闭，其余 8 条如实保留（启发式消歧、未穷举的 H5 换品、表达局限、无失败态等），均不阻塞交付。
