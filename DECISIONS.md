# DECISIONS —— 每条一行：定了什么 / 为什么 / 影响什么

- D001 工作目录 `report-generator/` 与材料目录平级、材料只读 / 交付自包含且不污染原始数据 / 所有代码引用相对路径 `../保险运营程序设计与实践_作业材料`，禁符号链接（Windows 交付机断链）
- D002 LLM 后端 MiMo，key 复制进 `config.yaml`（不走环境变量）/ 用户偏好 + hermes 已有凭证 / config.yaml 成唯一运行配置，重跑不依赖 shell 状态
- D003 探活选定 model=`mimo-v2.5-pro`（2026-09-24 实测 4 模型全 200：mimo-v2.5-pro / mimo-v2.5 / mimo-v2.6-pro / mimo-v2.6-flash；总控提示的 400 未复现，备选仍留档）/ 文档名不可信必须实测 / DECISIONS 留实测记录，失败时按备选顺序回退
- D004 MiMo 直连可用，代理 127.0.0.1:7897 作为配置项 fallback（不默认启用）/ 实测直连 200 / 少一次无谓跳转，网络变化时改 config 一行
- D005 M1 试点=鑫益延年(男,0,1)、对拍参考 HTML 后再跑 (男,30,10) / 参考成品 7 个数字与 JSON 逐字吻合是"证明读对数据"的最便宜方式 / M1 校验链带外部对照物，不自证
- D006 勾稽 A 口径：累计领取缺失/"-"按 0、满期年豁免公式计入分母 / 参考成品注释"60 岁前保证生存总利益=现金价值"，满期年现金价值已转满期金 / A=6720 与总控基线一致
- D007 勾稽 B 口径：严格 canonical 字段 `累积红利`，中意缺字段即跳过 / 纠偏一 canonical 4 字段、中意独有字段单列 / B=5760=6720−960 与总控基线一致；另实测中意别名口径 6720/6720 全过（记入验证报告）
- D008 勾稽 C 口径：保证/演示两档分别验、满期年计入、容差 0.011 / 源数据 2 位小数 / 单档 6300 与基线一致，双档合计 12600
- D009 红利 canonical=4 字段+中意单列可选模块 / 纠偏一 / 别名表不悄悄丢字段，展示层标注原名
- D010 断崖保留原值+标注，禁平滑禁修复 / 数据正确性优先 / 图表层禁止任何插值"美化"
- D011 长图降为可选，H5 必须真交互（切画像联动重排、切年度联动高亮）/ 总控拍板 / M4 验收含交互行为断言
- D012 讲义不 OCR（作业说明为权威规范），需要时仅 OCR 第三讲合规红线页 / 省工时 / 无
- D013 每个 LLM 调用=一个职责一个提示词文件、每产品/画像开新会话 / 防上下文污染 / prompts/ 目录 v1→vN 留档
- D014 提示词 v1→v2：v1 实测重试轮引入禁词"收益率"且年龄数字被回引误伤 → v2 增加"重试同样完整生效的硬性约束块 + IRR 固定写法 + 时点数字豁免 + 禁止自行断言领取截止" / 约束只在首版给出会在重试时失效 / v1 留档、v2 为 M1 终版
- D015 回引匹配用"去逗号 core 索引"统一匹配带/不带千分位、去尾零两种形态；年龄/年份整数豁免走 int 集合 / v1 实测 str/int 集合不匹配导致豁免失效、手动 setdefault 的带逗号值匹配不到 / trace 对所有值来源一致生效，M1 实测 unmatched=0
- D016 禁用词命中后加"合规改写重试≤2→再扫→终稿回引复核"通道 / 扫描只报告不修复会把违禁产出直接交出去 / 双通道（LLM 自检+代码词表）闭环，M1 实测 0 命中、改写轮 0 次
- D017 参考 HTML 对拍分"字面命中/数值关系"两列：HTML 展示存在 2 处舍入（2,819.63→2,820、101,590.06→10.16万），JSON 原值逐字核对 / 总控口径"逐字吻合"在 HTML 层面不严格成立，如实分列不掩盖 / 对拍表两列全 √ 才算通过
- D018 [M2 blocker] 档位归属表进 SPEC §2.5，摘要档位词由 tier_of() 机器生成、禁手写；tier_lint 断言"行文含演示⇒源字段必须在 红利.rate.*/IRR.hl/单利利率.hl 三条路径"否则抛 TierAssertionError / 顶层字段（养老金/累计领取等）是保证档，v1 摘要手写"演示领取合计"是摘要层污染、会通到所有下游 / 坏样本 2/2 拦截实测有效（validation/tier_assert_intercept.py）
- D019 求和值必须带代码产出的区间标签（起止 key+保单年度数），LLM 只准引用 / v1 喂错区间导致"到105岁"事实错误（实测 31500/700=45 年、60–104）/ 修正后终稿写"持续领取至104岁"
- D020 回引匹配键升级为（字段,key,档位）三元组，万元换算派生记录继承三元组并绑定发起字段，禁全局值池；同值多候选按语境消歧（万元→换算、紧邻倍→收益倍数、字段 hints 近邻），不唯一记 AMBIG 判失败 / 值对出处错是系统性问题（2.65、4.22 均张冠李戴）/ 对抗用例 8/8（validation/adversarial_trace.py），含"无语境同值必须 AMBIG"
- D021 免责声明由代码统一附加一次：生成后先剥除正文含"仅供教学研究使用/演示利益基于假设"的句子再追加固定免责块，断言两短语各出现 1 次 / v2 让 LLM 自己写免责导致正文+免责块重复 / 实测两场景各 1/1
- D022 提示词 v3：免责改由系统附加、合计必须带区间标签引用、档位照抄摘要标注 / 对应 D018–D021 三个修复 / v2 留档，v3 为当前版
- D023 收益倍数行单位从"元"改"倍"（add 支持 unit 参数）/ v3 摘要初版把倍数拼成"2.65元"属新引入单位错 / 修正后 2.65倍/4.22倍/10.16倍
- D024 画像按场景 meta 动态生成（≤12 岁走父母视角、成人走本人视角）/ 硬编码"0 岁男婴"曾配到 30 岁场景，终稿写出"0 岁宝宝年交10万" / 两场景画像与 meta 一致
- D025 摘要新增【保费（每年交费，区间+共N次）】行 + 提示词交费口径规则（年交只能引保费行、累计只能引累交保费）/ v3 把累计 100,000 说成"年交10万元"（实为 10,000）/ 重跑后年交 10,000 正确溯源 保费@31..40
- D026 输出层档位断言 output_tier_lint（窗口规则）：子句剥"不保证"后含"演示"→"演示"到随后"保证"字样之间窗口内数字必须 demo 档 / LLM 输出层把保证数字放进"根据演示"语境（摘要层断言管不到输出层）/ 违规句触发合规改写；整句含"保证"即跳过的初版规则漏掉真实违规句，窗口规则修复
- D027 免责去重升级：剥句范围扩到语义变体（不代表未来实际收益/红利分配不确定），断言改为"正文残留=0 + 免责块恰好 1 份"/ LLM 换措辞绕过字面子串、断言误把免责块自身短语计入 / 实测两场景 正文残留=0
- D028 提示词 v4：交费口径规则、纯文本禁 Markdown 符号、输出层档位显式约束 / 对应 D024-D027 / v3 留档
- D029 数字区间连字符归一（trace 匹配前 + sanitize 双处，ASCII hyphen→en dash，等长替换）/"60-104" 被正则切成 60 + "-104" 判无出处 / 单测+两场景复核通过
- D030 已知缺陷如实记录：回引在"同值跨量纲"（如 10 年 vs 10 万元换算）仍可能选错字段归属——值本身可溯源无假，但归属标注语义可能偏；语义级消歧列为 M2+ 改进项
- D031 [M2-A] 标量字段（投保年龄/交费期间/起领年龄）直接入回引池，kind='scalar'，消歧加"紧邻岁/年→标量优先" / 总控指出"持续交10年"归万元换算属巧合匹配（交费期间本就是 record 标量）/ 同句两个'10'实测分别归属 交费期间@10 与 累交保费@40(万元换算)
- D032 [M2-B] 合计类数字（is_range 记录：起止+年度数）必须同句带区间标签，range_binding_lint 并入 output_tier_lint，缺失即违规触发改写 / "没写错但脱离标签"正是当初 bug 的生长方式 / 坏样本"根据合同约定的领取区间，累计可领取31,500元"实测拦截
- D033 回引消歧五级规则定稿：①行内"源：X@"自证 ②紧邻万元→换算 ③紧邻倍→收益倍数 ④紧邻岁/年→标量 ⑤hints 打分唯一最高→平局按距离决胜→零信号且整数∈年龄域→时点豁免，仍不唯一记 AMBIG / 合成管线曾抓 12 个 AMBIG、对抗用例曾回归 7/8，逐案定位到平局与跨行邻域 / 终态：合成 76/0、对抗 8/8、lint 9/9
- D034 提示词 v2→v5：历史三次版本替换锚写成 'prompts/narrative_vX.md'，而代码是 os.path.join(ROOT,'prompts','narrative_vX.md') 分参数——静默未命中，M1 后期实际一直用 v2；v5 新增禁数据外功能断言（保单贷款类）、合计同句带区间 / 替换失败无报错是隐形事故，assert 化后才暴露 / 现行 v5，v1-v4 留档
- D035 场景优先级选择 bug：next(f if any endtail) 按文件序命中即返回，PRIORITY 排序无效——改为 tail 为外层循环；final_verify 打印对全角逗号文件名 IndexError——re.split 兼容半/全角 / 鸿盈/福临门/长城文件名逗号风格不同 / 修复后 7 产品均选中 30,10（长城 30,3）
- D036 M2 形态检测零人工分支：模块=字段存在性推断（可选模块清单），画像按 (年龄, 有无领取流) 四象限生成，产品由文件名反查目录 / 换产品零改码的证明 = 合成第 8 种字段组合全管线通过 + 7 产品同一 m1_run 管道出稿
- D037 [M3-①②] 给付流全枚举进 summary（年金/养老金/特别生存金 逐条 起领行+合计行，is_flow 登记），coverage_lint 断言三要素（起/止/年数/合计）全文必现，违规进合规改写循环 / 根因=原循环只取第一条流且 break、特别生存金从未入摘要——长城漏年金流(35–54/9820)、福临门漏特别生存金(37–39/30000)属给付结构缺失 / 坏样本用总控实抓旧稿原文，coverage_source_test 6/6（拦+放对）
- D038 [M3-③] sanitize 统一剥（源：…）括注、source_lint 断言终稿含"源："即违规；区间标签（区间 …）不含"源："保留 / 福临门稿曾泄漏9处内部源标注，其余8份0处属 sanitize 未覆盖该模式 / 断言入 output_tier_lint 统一由合规循环驱动
- D039 满期金单点流 range_info 只存 key 不存年数1、range_binding_lint/coverage_lint 按 trace 归属消歧 / 单点流曾强求裸"1"出现（系统性误伤）；保费10,000曾被误判为满期金合计句（同值无消歧）/ 修后 9/9 终验全过
- D040 提示词 v6：禁"保证利益演示"措辞（保证部分不得出现"演示"二字）/ LLM 曾用该词组让档位断言的窗口规则失效（3轮改写未收敛）/ 违规两份按 v6 重生成首版即过
- D041 m3_compare：reorder（模块顺序，结构化 JSON 数组，白名单排列校验，非法回退默认序，m_disclaimer 强制末位）+ narrative_modular（lead+每模块一句，JSON），模块数据块由代码从 rows 组装 / 两层分离要求顺序由 LLM 定、数字块不得经 LLM / reorder 实测输出合法排列；思考模型 max_tokens 必须给 reasoning 预算（300→3200 实测 finish_reason=length 截断致空回复）
- D042 git：config.yaml（含 API key）永不入库（.gitignore+config.example.yaml 模板）；重大步骤完成即 commit 并 push 到 GitHub / 总控要求 / remote=https://github.com/a161858970-ux/Insurance-Benefit-Illustration-Report-Generator
- D043 [M4] 内部 LLM 全量切换 mimo-v2.5-pro → mimo-v2.6-flash（总控指令）/ v2-pro 思考长、单次20-120s 是耗时主因之一 / 2026-09-24 实测探活：model_returned=mimo-v2.6-flash、最小调用 200、reasoning_tokens=12、中任务 2.8s；6 脚本+12 份报告按新模型全量重验
- D044 [M4] summary 新增保障责任行（身故首末年、全残首末年）标 is_resp，responsibility_lint 断言：字段存在则值必须出现在终稿——身故/全残由代码断言而非 LLM 自愿 / 总控 M4-1 / 并入 output_tier_lint 五道→六道统一通道
- D045 [M4] 9 份基础报告升级模块化（m_death/m_claims 进正式产物），m4_run 参数化复用 m3 管道，新画像 p_default 中性 / 单段报告身故/全残靠 LLM 自愿且模块块直拼数字无 record 不可回引 / 保障行入 records 后回引+断言双闭环
- D046 [M4] H5 数据 JSON 动态加载：data.json 由渲染层生成（模块文本+各画像 order/lead/句+年度序列带 data-source），页面零硬编码数值；切画像=reorder 输出重排 DOM，切年度=图表/表格高亮联动；可见文本抽取后复用同一套 lint（不另写）
- D047 趸交保费行 hints 加"趸交/当年"特征词 / 剥源后趸交保费与累交保费同值 hints 平局（块行 LLM 改不掉，3 轮改写全败）/ 修复后单场景 6.83s 首版全过
- D048 range_binding 跳过单点流（start==end，如满期金）/ 单点无区间歧义、coverage 已管呈现，曾强求同句写"105岁"致 3 轮改写不收敛 / 单点流走 coverage，求和流走区间绑定
- D049 trace 消歧全部规则限定 token 所在行内（规则0 源声明 field+key 精确、hints 打分、距离决胜）/ ±25 跨行邻域被上下行字段词双向污染（中意"生存总利益"块行被邻行"现金价值峰值""身故"搅成 AMBIG，改写修不掉因为是块行）/ 对抗8/8+6脚本全绿
- D050 flash 接入：enable_thinking=false 全局关思考（长约束 JSON 任务 reasoning 会爆 4384 token/130s，关后单次 ~7s），改写轮 per-call thinking=True 质量优先（关思考版改写 3 轮不收敛、开思考 1 轮过）/ config bool 解析曾把 'false' 当字符串致开关失效（bool('false')=True）/ 单份报告 130s→6.8s
- D051 9 份正式产物模块化（modular_run 参数化管道，p_default 中性画像），旧单段稿备份 narrative_final_单段版.txt；保障责任行 is_resp+responsibility_lint 使身故/全残由代码断言 / M4-1/2 / 身故 7/7、边界年 7/7、全残产物 3/3（中意/鑫颐金生/福临门）
- D052 H5：data.json 动态数据（模块行级 data-source、序列点级 source、关键值数字级 source）+ index.html 零硬编码（模板换 1e4 防误判）；可见文本复用同一套六道 lint；h5_lint_test 6 项（回溯 375/375+34/34、0 硬编码、lint 全0、data-source 机制、交互机制）/ M4-3/4
- D053 [M5] 数据来源+获取时点页脚：`compliance.footer_block(product,fn)` 生成两行，组装层代码拼接（assemble/m1_run/h5_build→data.json.footer），不走 LLM；`source_lint` 升级双向（剥页脚后查正文泄漏 + require_footer 查页脚存在），`output_tier_lint`/`trace` 内部先 strip_footer 豁免页脚出回引与档位窗口 / 作业合规清单第2条 12/12 全缺 + 实测三坑：页脚"数据来源："含裸"源："子串会被判泄漏、文件名"30，10"被 NUM_RE 并成无出处 token、"利益演示数据"含"演示"误触档位窗口且改写修不掉 / 坏样本 5 例（coverage_source_test ④），终验打印"页脚=√"
- D054 [M5] m3 对比组三份 mimo-v2.5-pro → mimo-v2.6-flash 全量重跑（总控拍板）；m3_compare 两处改写轮补 thinking=True（对齐 D050 分层）/ 交付物混两代模型说不清，且 m3 是个性化核心展示物；v2.5-pro 遗留是 README 已知缺陷第2条 / 12 份产物模型统一 flash，H5 的 order/lead/句随 m3 新产物重建
- D055 [M5] h5_build 产品/场景改命令行参数（默认原值），换产品时 m3 manifest 不匹配 → 画像回退默认序+空句；h5_lint_test 产品/场景从 data.json 自读 / H5 通用性需"换一款产品零改码"而原实现三处硬编码 / 通用性证明见 VERIFICATION.md（换前后页面标题+首行数字，index.html 字节不变）
- D056 [M5] 双防线收口：final_verify 增页脚断言并打印、m3/modular 终稿级 lint 开 require_footer=True（probe/片段级不开）、m1_run 组装后 assert 页脚存在 / 存在性断言必须落在"终稿级"调用点，开在片段级会把 lead/句判成缺页脚死循环 / 7 脚本 + 12 终验全绿
- D057 [M5] m3 对比组过滤 p_default（profiles.jsonl 标 compare:false）+ "3/3"硬编码字样改真值判定（len(results)==3 入 allok）/ p_default 是 M4 引入的中性画像、只服务 9 份正式产物，m3 重跑时未过滤多产第 4 份 cmp_p_default、且打印恒为"3/3 通过"会说谎 / 12 份口径恢复，重跑实测恰好 3 份
- D058 [M5] 句子级三查 probe_violations（禁词+档位窗口/区间/源泄漏+回引，剔除文档级 coverage/responsibility）：m3 probe 由"改一次不复检"改循环复检 ≤3 轮、modular_run 补句查轮、终态 ok 增 probe_ok、h5 sentences 只入会渲染模块（k in modules）+ 坏样本 3 例（⑤-a 实抓死角句 1.65 档位违规）/ module_block 对 m_disclaimer 恒返 None → 该句从不渲染、终验只查渲染正文，p_analyst 的 m_disclaimer 句带 tier 违规入库，h5_build 汇总可见文本才暴雷（lint 死角）/ 12 份 manifest 句级重扫 1 脏（p_analyst）→ 重跑该份后归净
- D059 [M5] 句查轮改写固定 thinking=False / 实测同调用 thinking=True 495.2s vs False 6.0s（输出质量相当）：句查是单句 surgical 修改、非整稿长 JSON 重写，D050"改写开思考"不适用此场景，曾两次误判挂死并杀进程 / 单份句查改写 495s→6s，整稿终验改写仍保持开思考
- D060 [终验] 合规清单第 5 条"成果不对外传播、不用于实际销售"按项目方判断认定为 √，判断依据写入 VERIFICATION 第 7 章第 5 条 / 该条原写"产物仅存本地 git 仓库，未发布"与事实不符（仓库已 push 且为 GitHub Public，总控实测页面徽章 Public、isPrivate=false），必须改为如实陈述 + 判断依据 / 事实核查：135 个入库文件中 `利益演示数据/` 原始 JSON **0 个**（数据源未入库），公开的是代码/文档/提示词与由代码生成的 `output/` 24 个文件（含 H5 data.json 的派生数字）；公开用途是提交方式（老师以仓库链接收作业），不作宣传、无对外发布渠道、12 份报告与 H5 页脚均带"仅供教学研究使用"
- D061 [终验] 文档语态由"已交付待总控终验"统一更新为"已完结、总控终验通过"，VERIFICATION 增第 9 章终验结论、README 增"这是什么"项目介绍、CHANGE_LOG 增终验条目并标注历史状态已过期 / 项目完结后文档不应停留在待验收态 / 语态更新不改动任何技术内容与校验数字
