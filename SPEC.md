# SPEC —— 唯一真相来源（Single Source of Truth）

字段定义、口径、勾稽公式、禁用词、模块 ID 只在本文件定义；其他代码/提示词一律引用本文件，不得各自另立定义。

## 1. 数据格式

- 路径：`../保险运营程序设计与实践_作业材料/利益演示数据/<产品目录>/<产品名>，性别，年龄，交费期间.json`
- 每文件是 `list`，长度 1；`record = json.load(f)[0]`
- **key = 该保单年度末的被保险人年龄**（不是保单年度）。30 岁投保 `"31"` = 第 1 个保单年度末。保单年度 = key − 投保年龄。
- 所有值是**字符串**；`"-"` = 不适用（口径性无值，非缺失、非 0）
- 标量字段：`性别`、`年龄`（投保年龄）、`交费期间`
- 全部产品保障至 105 岁（key 最大值恒为 `105`）

## 2. 字段清单与 canonical 映射（红利纠偏一）

### 2.1 通用标量
`性别 / 年龄 / 交费期间`

### 2.2 年度序列（key=年龄的 dict）
必有：`保费`、`累交保费`、`现金价值`、`身故保险金`、`IRR.{bzhl,hl}`、`单利利率.{bzhl,hl}`
可选（按产品形态自动检测，缺失即无此模块，禁止人工分支）：
`养老金`、`年金`、`满期金`、`特别生存金`、`累计领取`、`领取年龄`、`全残保险金`

### 2.3 红利组 `红利.{guaranteed, rate}`
**canonical 4 字段**（B/C 勾稽只认这 4 个名字）：

| canonical | 6 款产品字段名 | 中意字段名 |
|---|---|---|
| 当年度红利 | `当年度红利` | `年度红利保额` |
| 累积红利 | `累积红利` | **无此字段** |
| 生存总利益 | `生存总利益` | `生存总利益` |
| 收益倍数 | `收益倍数` | `收益倍数` |

- 中意独有 `累积红利保额现价` / `累积红利保额`：**单列为可选模块**（中意的"累积红利"语义 = 累积红利保额现价），不进 canonical 4 字段表；B 勾稽的基线口径用严格 canonical（中意因此跳过，见 §3），展示层可用别名读取但必须标注字段原名。

### 2.4 口径缩写
`bzhl` = 保证（不含红利）；`hl` = 含红利（演示）。guaranteed/rate 同义：guaranteed=保证，rate=演示（按红利假设）。

### 2.5 档位归属（每个年度序列字段唯一归属，M2 起摘要档位标签只能由本表机器生成）

| 档位 | 字段 |
|---|---|
| **保证档** | 顶层全部序列：`保费`、`累交保费`、`现金价值`、`身故保险金`、`全残保险金`、`养老金`、`年金`、`满期金`、`特别生存金`、`累计领取`、`领取年龄`；`红利.guaranteed.*`；`IRR.bzhl`；`单利利率.bzhl` |
| **演示档** | 仅三条路径：`红利.rate.*`、`IRR.hl`、`单利利率.hl` |

**档位标注断言（blocker 级）**：凡行文含"演示"二字的数字行，其源字段路径必须 ∈ 演示档三条路径，否则渲染层直接抛 `TierAssertionError`——"保证标成演示"与"演示说成保证"同属红线。摘要行的档位词（保证/演示、不保证）由 `tier_of()` 按本表生成，**禁止在 label/正文字面手写档位字样**。
**求和区间断言**：任何求和值必须由代码同时产出区间标签（起止 key + 保单年度数，如"60–104 岁、共 45 个保单年度"），LLM 只准引用该标签，禁止自行概括截止年龄。
**回引出处三元组**：每个可引用值登记为 `(字段, key, 档位)` 记录；万元换算派生独立记录并继承三元组、绑定发起换算的字段（标 `wanyuan`），**禁止全局值池匹配**；同值多候选时按语境消歧（"万元"→换算记录、紧邻"倍"→收益倍数字段、字段名近邻命中），消歧不唯一记 AMBIG 判失败。


## 3. 勾稽公式（口径与总控基线一致：A=6720、B=5760、C=6300、失败=0）

| 编号 | 公式 | 口径细则 |
|---|---|---|
| **A** | `生存总利益(guaranteed) = 现金价值 + 累计领取` | `累计领取` 字段缺失或值为 `"-"`（未起领）→ 按 **0** 计（即保证生存总利益=现金价值，参考成品注释同口径）；满期年（key=105）**豁免公式但计入分母**（满期时现金价值已转满期金计入累计领取，再加即重复）；任一参与项非数值才跳过。 |
| **B** | `生存总利益(rate) = 生存总利益(guaranteed) + 累积红利(rate)` | `累积红利` 用**严格 canonical 字段名**：中意无此字段 → 该产品全部 key 跳过（这正是 B=5760=6720−960 的来源）；满期年不豁免（该恒等式满期年仍成立）；`"-"` 跳过。 |
| **C** | `收益倍数 = 生存总利益 ÷ 累交保费` | 保证、演示**两档分别验**，各自计数相同（单档基线 6300）；满期年**计入**；容差 0.011（源数据 2 位小数）；`"-"` 跳过；`累交保费=0` 跳过。 |

A 的分母恒等于全量 key 数：7 产品 × 4 场景组 × (105+75+60) = 6720。

**通过判据（M2 起全量）：A=6720/6720、B=5760/5760、C=6300/6300（单档口径），失败=0。** 口径差异必须先解释再改码，禁止调口径凑数。

## 4. 五类边界年（逐年可查）

每场景文件必须扫描并列出：
1. **首年** = key(投保年龄+1)
2. **缴费期满年** = key(投保年龄+交费期间)（趸交=交费期间1，与首年重合）
3. **起领年** = 有 `领取年龄` 字段取该值；否则=首个 `养老金`/`年金` 非 `"-"` 的 key
4. **满期年** = 最大 key（105）
5. **给付比例变化年** = `身故保险金` 相对前一年下降的全部 key（逐年列出）

## 5. 断崖纪律
身故金随年龄递减（保额比例要求下调）是真实口径，**图表保留原值 + 标注，禁止平滑、禁止修复**；文案解释只能引用给定口径，不得改动数字。

## 6. 数字纪律（全项目最高优先级）

- 任何数字只在渲染层产生、计算、格式化；LLM 输入=代码生成的数据摘要，输出=叙述文本+模块 ID 数组。
- LLM 输出必须通过**数字回引校验**：输出中每个数字（含万元换算表述）必须命中摘要中的源值，否则重试 ≤2 次，仍失败降级为占位符 `{{N:字段@key}}`。
- 禁止 LLM 重算/推算/四舍五入/凑整/换单位；**IRR（复利）是唯一收益口径**；单利、收益倍数展示必须标明口径。
- 保证（guaranteed/bzhl）与演示（rate/hl）两档分开表述，演示档必须标"演示、不保证"。

## 6.5 覆盖与源标注（M3 断言）
- **给付流覆盖 coverage_lint**：产品全部非"-"给付流（年金/养老金/特别生存金/满期金）每条以 `(起止key, 年数, 合计)` 登记；起、止/年数、合计三类要素必须出现在终稿全文，缺一即违规触发改写。多条流一条都不能漏（如长城 年金35–54 与 养老金55–105 必须都呈现）。
- **源标注 source_lint**：`（源：…）`括注只许存在于摘要/验证层；终稿出现"源："即违规。sanitize 统一剥除；区间标签`（区间 …）`不含"源："必须保留（B 条依赖）。
- **身故/全残呈现**：按"断崖年+关键年（首年末/末年）"给值，不做逐年合计（非领取流，不进 coverage）。

## 7. 禁用词表（代码扫描，0 命中）
`收益率、理财、存款、保本、稳赚、保底收益、稳健增值、无风险、推荐、建议购买、适合您`

## 8. 展示数字回溯
页面/报告中每个展示数字带 `data-source="字段@key"`，抽查脚本据此回溯源 JSON。

## 9. 模块 ID 表（M3 个性化重排用；LLM reorder 只准输出本表 ID 的排列）
`m_basics` 投保方案 / `m_cashflow` 领取现金流 / `m_guarantee` 保证利益 / `m_dividend` 红利演示 / `m_irr` 收益口径(IRR/单利/倍数) / `m_death` 身故保障 / `m_claims` 给付边界年 / `m_disclaimer` 数据来源与免责

## 10. 数据来源与时点
数据 = 课程作业材料 `利益演示数据/` 目录给定 JSON；获取时点 = 材料发布时点；每份产出必附免责声明（教学研究用途、演示利益基于假设、红利不确定、以正式条款及保单为准）。

**落点（M5，作业合规清单第 2 条"标注了数据来源和获取时点"）**：
- 12 份 txt 终稿页脚两行，由渲染层代码拼接（与免责块同逻辑，不走 LLM），格式固定：
  ```
  数据来源：课程作业材料《利益演示数据/》给定 JSON（产品名，场景文件名）
  获取时点：材料发布时点
  ```
  拼接点：模块化=`report.assemble()`（m3/m4 共用）、单段=`m1_run` 组装后、H5=`h5_build` 写入 `data.json.footer` 由 index.html 页脚渲染。
- 断言：`source_lint(require_footer=True)`——终稿缺任一行即违规；该断言由 `output_tier_lint(..., require_footer=True)` 携带，终验（final_verify）与管道终稿级 lint 必开；坏样本见 `validation/coverage_source_test.py` ④。
- 页脚是代码拼接的标识文本，**豁免出回引/档位窗口/区间/覆盖/责任检查**（`trace` 与 `output_tier_lint` 内部先 `strip_footer`）：页脚含场景文件名数字（无摘要出处）与"利益演示数据"字样（含"演示"，会误触档位窗口），且 LLM 改写修不掉代码拼接的内容。存在性另行断言，双向不漏。
